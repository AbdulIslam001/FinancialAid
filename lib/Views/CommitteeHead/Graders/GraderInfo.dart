
import 'package:flutter/material.dart';

class GraderInfo extends StatelessWidget {

  const GraderInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:const Text("Graders")),
      body: Column(
        children: [

        ],
      ),
    );
  }
}
